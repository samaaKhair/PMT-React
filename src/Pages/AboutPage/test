/* Apply background images as header and footer */
.about-page {
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  align-items: center;
  min-height: 100vh;
  position: relative; /* Added position relative */
}

/* Style the "About Us" image */
.aboutUs {
  width: 100%;
  height: 7rem;
  z-index: 1; /* Place image above text and icons */
  position: relative; /* Added position relative for text overlay */
}

/* Style the "Contact Us" image */
.contactUs {
  width: 100%;
  height: 7rem;
  z-index: 1; /* Place image above text and icons */
}

/* Style the "About Us" text */
.about-text {
  position: absolute; /* Position absolute for overlay */
  top: 50%; /* Vertically center */
  left: 50%; /* Horizontally center */
  transform: translate(-50%, -50%); /* Center the text */
  z-index: 2; /* Place text above images */
  color: white; /* Text color */
  font-size: 24px; /* Font size */
  font-weight: bold; /* Font weight */
}

/* Style the "Contact Us" text */
.contact-text {
  position: absolute; /* Position absolute for overlay */
  bottom: 10px; /* Adjust as needed */
  left: 50%; /* Horizontally center */
  transform: translateX(-50%); /* Center horizontally */
  z-index: 2; /* Place text above images */
  color: white; /* Text color */
  font-size: 24px; /* Font size */
  font-weight: bold; /* Font weight */
}

/* Style the contact icons */
.contact-icons {
  position: absolute; /* Position absolute for overlay */
  bottom: 50px; /* Adjust as needed */
  left: 50%; /* Horizontally center */
  transform: translateX(-50%); /* Center horizontally */
  z-index: 2; /* Place icons above images */
}

/* Style individual icons */
.contact-icons i {
  font-size: 24px;
  margin: 10px;
  color: white; /* Icon color */
  background-color: #333; /* Icon background color */
  border-radius: 50%; /* Make the icons circular */
  padding: 10px;
}
